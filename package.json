{
  "name": "kibana_dashboard_custom_css",
  "version": "0.1.0",
  "description": "A Plugin for Kibana 5.x that allows to inject a custom css file as query param to style dashboards.",
  "main": "index.js",
  "kibana": {
    "version": "5.2.3"
  },
  "scripts": {
    "lint": "eslint",
    "start": "plugin-helpers start",
    "test:server": "plugin-helpers test:server",
    "test:browser": "plugin-helpers test:browser",
    "build": "plugin-helpers build",
    "postinstall": "plugin-helpers postinstall"
  },
  "devDependencies": {
    "@elastic/eslint-config-kibana": "0.0.2",
    "@elastic/plugin-helpers": "^6.0.0",
    "babel-eslint": "4.1.8",
    "chai": "^3.5.0",
    "eslint": "1.10.3",
    "eslint-plugin-mocha": "1.1.0"
  }
}
